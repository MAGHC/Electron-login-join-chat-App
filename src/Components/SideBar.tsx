import Box from "@mui/material/Box";

const SideBar = () => {
  return (
    <Box
      sx={{
        width: "20rem",
        height: "100vh",
        backgroundColor: "primary.dark",
      }}
    ></Box>
  );
};

export default SideBar;
